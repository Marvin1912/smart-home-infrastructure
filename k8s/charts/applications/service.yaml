apiVersion: v1
kind: Service
metadata:
  name: applications-service
  namespace: default
  labels:
    app: applications-service
spec:
  selector:
    app: applications
  ports:
    - name: http-web
      protocol: TCP
      port: 9055
      targetPort: 9055
  type: ClusterIP
