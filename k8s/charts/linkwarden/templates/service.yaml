apiVersion: v1
kind: Service
metadata:
  name: linkwarden
  namespace: default
  labels:
    app: linkwarden
spec:
  selector:
    app: linkwarden
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  type: {{ .Values.service.type }}
