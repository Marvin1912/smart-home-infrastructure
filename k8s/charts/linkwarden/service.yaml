apiVersion: v1
kind: Service
metadata:
  name: linkwarden
  namespace: default
  labels:
    app: linkwarden
spec:
  selector:
    app: linkwarden
  ports:
    - name: http
      port: 3000
      targetPort: http
  type: ClusterIP
