apiVersion: v1
kind: Service
metadata:
  name: wikijs-service
  namespace: default
  labels:
    app: wikijs-service
spec:
  selector:
    app: wikijs
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
