apiVersion: v1
kind: Service
metadata:
  name: wikijs-service
  namespace: default
  labels:
    app: wikijs-service
spec:
  selector:
    app: wikijs
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
