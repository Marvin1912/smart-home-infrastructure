apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  namespace: default
  labels:
    app: grafana
spec:
  config:
    security:
      cookie_samesite: {{ .Values.grafana.cookieSamesite }}
      allow_embedding: {{ .Values.grafana.allowEmbedding | quote }}
  deployment:
    spec:
      template:
        spec:
          containers:
            - name: grafana
              env:
                - name: GF_SECURITY_ADMIN_USER
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.grafana.adminUserSecret.name }}
                      key: {{ .Values.grafana.adminUserSecret.key }}
                - name: GF_SECURITY_ADMIN_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.grafana.adminPasswordSecret.name }}
                      key: {{ .Values.grafana.adminPasswordSecret.key }}

