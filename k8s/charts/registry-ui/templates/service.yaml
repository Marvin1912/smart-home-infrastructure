apiVersion: v1
kind: Service
metadata:
  name: registry-ui
  namespace: default
  labels:
    app: registry-ui
spec:
  clusterIP: None
  ports:
  - name: http
    port: {{ .Values.registryUi.port }}
    targetPort: {{ .Values.registryUi.port }}
