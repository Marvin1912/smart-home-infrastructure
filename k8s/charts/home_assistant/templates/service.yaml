apiVersion: v1
kind: Service
metadata:
  name: home-assistant
  namespace: default
  labels:
    app: home-assistant
spec:
  clusterIP: None
  ports:
  - name: metrics
    port: {{ .Values.homeAssistant.metricsPort }}
    targetPort: {{ .Values.homeAssistant.metricsPort }}
