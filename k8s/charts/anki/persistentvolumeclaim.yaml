apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-mount-anki
spec:
  storageClassName: local-mount
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: local-mount-anki