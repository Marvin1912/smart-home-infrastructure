apiVersion: v1
kind: Service
metadata:
  name: anki-service
  namespace: default
  labels:
    app: anki-service
spec:
  selector:
    app: anki
  ports:
    - name: http-web
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
