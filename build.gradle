plugins {
    id 'java'
}

ext {
    registry = project.registry_local
}

// This project now serves as a configuration repository for smart home infrastructure.
// Docker images are built and managed separately through the docker-compose setup.
// The Gradle build is maintained for potential future custom tooling and utilities.
